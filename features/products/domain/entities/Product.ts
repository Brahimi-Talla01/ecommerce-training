/**
 * MISSION 1 - ÉTAPE 1
 * 
 * Créer la classe Product avec les propriétés suivantes :
 * - id: string
 * - name: string
 * - description: string
 * - price: number
 * - imageUrl: string
 * - category: string
 * - stock: number
 * - createdAt: Date
 * 
 * Ajouter une méthode :
 * - isAvailable(): boolean (retourne true si stock > 0)
 * - getFormattedPrice(): string (retourne "45 000 FCFA")
 */

export class Product {
  // TODO: Implémenter la classe Product
}